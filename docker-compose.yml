services:
  database:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: fcacp
      POSTGRES_PASSWORD: super-secure-password
    ports:
      - protocol: tcp
        target: 5432
        published: 5432
    volumes:
      - type: volume
        source: database
        target: /var/lib/postgresql

volumes:
  database:
